#@ load("@ytt:data", "data")
#@ load("@ytt:base64", "base64")
apiVersion: v1
kind: Secret
metadata:
  name:  kibana-basic-auth
  namespace: #@ data.values.namespace
data:
  auth: #@ base64.encode(data.values.logging.kibana.htpasswd)
type: Opaque
